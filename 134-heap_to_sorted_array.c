#include "binary_trees.h"

/**
* heap_to_sorted_array - It's a pending.
* @heap:It's  pending.
* @size: It's pending.
* Return: It's pending.
*/
int *heap_to_sorted_array(heap_t *heap, size_t *size)
{
	(void) size;
	if (!heap)
		return (NULL);
	return (NULL);
}
