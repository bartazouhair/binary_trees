#include "binary_trees.h"

/**
* avl_remove - It's pending.
* @root: It's pending.
* @value: It's pending.
* Return: It's pending.
*/
avl_t *avl_remove(avl_t *root, int value)
{
	if (!root)
		return (NULL);
	return (root);
}
